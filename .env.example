# --- LLM Provider ---
# Optional: If unset, Celery tasks will fail on real execution but API tests will pass.
# For a full end-to-end run, you must provide a valid Gemini API key.
GOOGLE_API_KEY=

# --- X / Twitter API (for x_research_tool) ---
# Optional: If unset, the Trend Hunter agent will receive mock Twitter data instead of live tweets.
X_BEARER_TOKEN=
